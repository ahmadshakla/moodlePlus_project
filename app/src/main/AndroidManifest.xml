<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".MoodleApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_icon1"
        android:label="Moodle+"
        android:roundIcon="@mipmap/ic_icon1_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:label">
        <activity android:name=".CourseInformation.AssignmentSubmissions.AssignmentStatusActivity"></activity>
        <activity android:name=".CourseInformation.AssignmentSubmissions.AssignmentSubmissionActivity" />
        <activity android:name=".CourseInformation.CourseFolderViewActivity" />
        <activity android:name=".CourseInformation.CourseEvents.CourseEventsActivity" />
        <activity android:name=".CourseInformation.CourseForums.ReplyToPostActivity" />
        <activity android:name=".CourseInformation.CourseForums.AddDiscussionActivity" />
        <activity android:name=".CourseInformation.CourseForums.DiscussionPostsActivity" />
        <activity android:name=".CourseInformation.CourseForums.ForumViewActivity" />
        <activity android:name=".CalenderData.CalenderViewActivity" />
        <activity android:name=".CourseInformation.CourseGrades.GradesInfoActivity" />
        <activity
            android:name=".CourseInformation.CourseInfoActivity"
            tools:ignore="InnerclassSeparator" />
        <activity
            android:name=".MainMenu.CoursesMenuActivity"
            tools:ignore="InnerclassSeparator" />
        <activity android:name=".MainActivity">
            <intent-filter tools:ignore="ExtraText">
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>